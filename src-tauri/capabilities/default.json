{
  "identifier": "default",
  "description": "Default capabilities for Claude Desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "dialog:allow-open",
    "core:event:default",
    "core:event:allow-listen",
    "core:event:allow-emit"
  ]
}
